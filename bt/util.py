def pct_change(initial, final):
    """
    Calculate the percentage change between two numbers.

    :param initial: The first number.
    :param final: The second number.
    :return: The percentage change between a and b.
    """
    return ((final - initial) / initial) * 100
